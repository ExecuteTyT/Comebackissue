# 🛡️ вернистраховку.рф

Лендинг для возврата навязанных банковских услуг - профессиональный сайт ООО "ПЕРВЫЙ"

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Node.js Version](https://img.shields.io/badge/node-%3E%3D16.0.0-brightgreen)](https://nodejs.org/)

## 📋 Содержание

- [Описание](#-описание)
- [Возможности](#-возможности)
- [Технологии](#-технологии)
- [Установка](#-установка)
- [Конфигурация](#-конфигурация)
- [Запуск](#-запуск)
- [Структура проекта](#-структура-проекта)
- [Безопасность](#-безопасность)
- [SEO и оптимизация](#-seo-и-оптимизация)
- [Деплой](#-деплой)
- [Разработка](#-разработка)

## 🎯 Описание

Полнофункциональный лендинг для юридической компании, специализирующейся на возврате навязанных страховок и услуг по кредитам. Сайт включает:

- Адаптивный дизайн для всех устройств
- Калькулятор возврата денег
- Формы обратной связи с валидацией
- Интеграция с email и Telegram
- Защита от спама и атак
- SEO оптимизация

## ✨ Возможности

### Frontend
- ✅ Полностью адаптивный дизайн (mobile-first)
- ✅ Калькулятор возврата с анимациями
- ✅ 4 формы обратной связи
- ✅ Отзывы клиентов с Swiper
- ✅ FAQ аккордеон
- ✅ Плавные анимации (AOS)
- ✅ Модальные окна
- ✅ Exit-intent popup
- ✅ Маски ввода телефона

### Backend
- ✅ Express.js сервер
- ✅ Rate limiting (защита от спама)
- ✅ CSRF защита
- ✅ Валидация и санитизация данных
- ✅ Email уведомления (Nodemailer)
- ✅ Telegram уведомления (Bot API)
- ✅ Логирование (Winston)
- ✅ CORS настройки
- ✅ Helmet (security headers)

### SEO & Analytics
- ✅ Schema.org разметка
- ✅ robots.txt
- ✅ sitemap.xml
- ✅ Yandex.Metrika готов к интеграции
- ✅ Open Graph теги
- ✅ Оптимизированная загрузка шрифтов

## 🛠 Технологии

**Frontend:**
- HTML5, CSS3 (с Tailwind CSS)
- JavaScript (ES6+)
- Swiper.js - слайдер отзывов
- AOS - анимации при скролле
- IMask.js - маски ввода
- Font Awesome - иконки

**Backend:**
- Node.js >= 16.0.0
- Express.js 4.18.2
- Nodemailer 7.0.10 (безопасная версия)
- Winston - логирование
- express-rate-limit - защита от спама
- express-validator - валидация
- helmet - security headers
- csrf-csrf - CSRF защита
- DOMPurify - санитизация HTML

## 📦 Установка

### Требования

- Node.js >= 16.0.0
- npm >= 8.0.0
- SMTP сервер для отправки email
- Telegram Bot Token (опционально)

### Шаги установки

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/yourusername/verni-strahovku.git
cd verni-strahovku
```

2. **Установите зависимости:**
```bash
npm install
```

3. **Создайте файл .env:**
```bash
cp .env.example .env
```

4. **Настройте переменные окружения в .env** (см. раздел [Конфигурация](#-конфигурация))

## ⚙️ Конфигурация

### Обязательные настройки

Отредактируйте файл `.env` и заполните следующие переменные:

#### 1. Email настройки

```env
SMTP_HOST=smtp.spaceweb.ru
SMTP_PORT=465
EMAIL_USER=your_email@inbox.ru
EMAIL_PASS=your_password_here
NOTIFICATION_EMAIL=your_email@inbox.ru
```

**Как получить:**
- Используйте любой SMTP сервер (Spaceweb, Gmail, Mail.ru, Yandex, SendGrid, etc.)
- Для Spaceweb: используйте порт 25, 2525 или 465 (SSL)
- Для Gmail: включите "Доступ ненадежных приложений" или используйте App Password
- Для Mail.ru/Inbox.ru: используйте обычный пароль

#### 2. Telegram Bot настройки

```env
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
TELEGRAM_CHAT_ID=123456789
```

**Как получить:**

1. **Создайте бота:**
   - Откройте [@BotFather](https://t.me/BotFather) в Telegram
   - Отправьте `/newbot`
   - Следуйте инструкциям
   - Скопируйте токен бота

2. **Получите Chat ID:**
   - Напишите боту любое сообщение
   - Откройте [@userinfobot](https://t.me/userinfobot) или [@RawDataBot](https://t.me/RawDataBot)
   - Скопируйте ваш Chat ID

#### 3. Безопасность

```env
CSRF_SECRET=your-random-secret-key-change-this-123456789
```

Сгенерируйте случайную строку:
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

#### 4. CORS настройки

```env
ALLOWED_ORIGINS=https://вернистраховку.рф,https://xn--80aaffoba4b4cyb.xn--p1ai
```

Для локальной разработки:
```env
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
```

#### 5. Yandex.Metrika (опционально)

```env
YANDEX_METRIKA_ID=12345678
```

Создайте счетчик на [https://metrika.yandex.ru/](https://metrika.yandex.ru/)

### Дополнительные настройки

```env
# Порт сервера
PORT=3000

# Окружение (development | production)
NODE_ENV=development

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000     # 15 минут в миллисекундах
RATE_LIMIT_MAX_REQUESTS=100     # Макс. запросов с одного IP
```

## 🚀 Запуск

### Режим разработки

```bash
npm run dev
```

Сервер запустится на http://localhost:3000 с автоматической перезагрузкой при изменении файлов (nodemon).

### Режим production

```bash
npm start
```

### Проверка работоспособности

1. Откройте http://localhost:3000
2. Проверьте health check: http://localhost:3000/api/health
3. Попробуйте отправить тестовую форму

## 📁 Структура проекта

```
verni-strahovku/
├── backend/
│   └── server.js              # Express сервер с защитами
├── src/
│   ├── css/
│   │   └── style.css          # Кастомные стили
│   └── js/
│       ├── main.js            # Основная логика frontend
│       └── calculator.js      # Калькулятор возврата
├── assets/
│   ├── logo-main.svg          # Логотип
│   ├── favicon.svg            # Favicon
│   └── site.webmanifest       # PWA манифест
├── logs/                      # Логи сервера (создается автоматически)
│   ├── error.log              # Логи ошибок
│   └── combined.log           # Все логи
├── index.html                 # Главная страница
├── privacy.html               # Политика конфиденциальности
├── robots.txt                 # Правила для поисковых роботов
├── sitemap.xml                # Карта сайта
├── .env                       # Переменные окружения (НЕ коммитить!)
├── .env.example               # Пример .env файла
├── .gitignore                 # Git ignore правила
├── package.json               # NPM зависимости
└── README.md                  # Этот файл
```

## 🔒 Безопасность

### Реализованные защиты

1. **Rate Limiting:**
   - Общий лимит: 100 запросов за 15 минут
   - Лимит форм: 5 отправок за 15 минут

2. **CSRF Protection:**
   - Double Submit Cookie метод
   - Токены для всех POST запросов

3. **Валидация и санитизация:**
   - express-validator для проверки данных
   - DOMPurify для очистки HTML
   - Регулярные выражения для форматов

4. **Security Headers (Helmet):**
   - Content Security Policy
   - X-Frame-Options
   - X-Content-Type-Options
   - и другие

5. **CORS:**
   - Whitelist разрешенных доменов
   - Credentials support

### Рекомендации для production

1. **HTTPS обязателен:**
   - Используйте SSL/TLS сертификаты
   - Настройте редирект с HTTP на HTTPS

2. **Обновляйте зависимости:**
```bash
npm audit
npm update
```

3. **Используйте PM2 для production:**
```bash
npm install -g pm2
pm2 start backend/server.js --name verni-strahovku
pm2 startup
pm2 save
```

4. **Настройте firewall:**
   - Откройте только порты 80 и 443
   - Закройте порт 3000 для внешнего доступа

5. **Мониторинг логов:**
```bash
# Просмотр логов
tail -f logs/combined.log
tail -f logs/error.log

# С PM2
pm2 logs verni-strahovku
```

## 📈 SEO и оптимизация

### Реализовано

- ✅ **Schema.org разметка** - структурированные данные для поисковиков
- ✅ **robots.txt** - правила индексации
- ✅ **sitemap.xml** - карта сайта
- ✅ **Open Graph теги** - для соцсетей
- ✅ **Оптимизированная загрузка шрифтов** - async с fallback
- ✅ **Семантическая разметка** - правильные HTML5 теги
- ✅ **Meta теги** - описание, keywords

### Checklist перед запуском

- [ ] Заполните YANDEX_METRIKA_ID в .env
- [ ] Проверьте Open Graph изображение (og:image)
- [ ] Отправьте sitemap.xml в Яндекс.Вебмастер и Google Search Console
- [ ] Настройте Google Analytics (опционально)
- [ ] Проверьте скорость загрузки в PageSpeed Insights

## 🌐 Деплой

### VPS/Dedicated Server

1. **Подготовьте сервер:**
```bash
# Обновите систему
sudo apt update && sudo apt upgrade -y

# Установите Node.js 16+
curl -fsSL https://deb.nodesource.com/setup_16.x | sudo -E bash -
sudo apt-get install -y nodejs

# Установите Nginx
sudo apt install nginx -y

# Установите PM2
sudo npm install -g pm2
```

2. **Клонируйте проект:**
```bash
cd /var/www
git clone your-repo.git verni-strahovku
cd verni-strahovku
npm install --production
```

3. **Настройте .env** (см. [Конфигурация](#-конфигурация))

4. **Запустите с PM2:**
```bash
pm2 start backend/server.js --name verni-strahovku
pm2 startup
pm2 save
```

5. **Настройте Nginx как reverse proxy:**
```nginx
server {
    listen 80;
    server_name вернистраховку.рф xn--80aaffoba4b4cyb.xn--p1ai;

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

6. **Получите SSL сертификат:**
```bash
sudo apt install certbot python3-certbot-nginx
sudo certbot --nginx -d вернистраховку.рф -d xn--80aaffoba4b4cyb.xn--p1ai
```

### Vercel / Netlify

Проект оптимизирован для деплоя на VPS. Для Vercel/Netlify потребуется адаптация backend.

## 👨‍💻 Разработка

### Git Flow

```bash
# Создайте feature branch
git checkout -b feature/new-feature

# Коммитьте изменения
git add .
git commit -m "feat: add new feature"

# Пушьте в репозиторий
git push origin feature/new-feature
```

### Полезные команды

```bash
# Проверка кода
npm run lint          # ESLint (если настроен)

# Тесты
npm test

# Сборка для production
npm run build         # (если настроена)

# Очистка node_modules
rm -rf node_modules
npm install
```

### Отладка

**Backend логи:**
```bash
# В режиме разработки логи выводятся в консоль
npm run dev

# В production логи сохраняются в файлы
tail -f logs/combined.log
```

**Frontend отладка:**
Откройте DevTools (F12) и проверяйте консоль на ошибки.

## 📝 Лицензия

MIT License - см. [LICENSE](LICENSE)

## 👥 Авторы

**ОБЩЕСТВО С ОГРАНИЧЕННОЙ ОТВЕТСТВЕННОСТЬЮ "ПЕРВЫЙ"**
- ИНН: 1650448426
- ОГРН: 1027700067328
- Юр. адрес: 423810, Республика Татарстан, г. Набережные Челны, пр-кт Хасана Туфана, дом 12, офис 1506
- Email: delovoi_podhod@inbox.ru
- Телефон: 8-904-666-66-46
- Сайт: https://вернистраховку.рф

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:

1. Проверьте [FAQ](#) в документации
2. Откройте Issue в GitHub
3. Свяжитесь с разработчиками

## 📚 Дополнительные ресурсы

- [Express.js документация](https://expressjs.com/)
- [Tailwind CSS документация](https://tailwindcss.com/)
- [Yandex.Metrika](https://metrika.yandex.ru/)
- [Schema.org](https://schema.org/)

---

**Сделано с ❤️ для вернистраховку.рф**
